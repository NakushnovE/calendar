export interface Dates {
    classes: string;
    d: number | any;
    active: boolean;
}

export interface Weekday {
    letter: string;
}
export interface Modal {
    visible: boolean,
    title: string,
    description: string
}
export interface IOp {
    type: string
}